<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scope X</title>
    <meta name="description" content="Scope X - Plataforma de inicio de sesión">
    <meta name="keywords" content="Scope X, login, plataforma">
    <meta name="author" content="Tu Nombre">
    <style>
        :root {
            --primary-color: #007BFF;
            --secondary-color: #0056b3;
            --background-color: black;
            --text-color: white;
        }

        @keyframes neon {
            0%, 100% {
                text-shadow: 0 0 5px #ff073a, 0 0 10px #ff073a, 0 0 15px #ff073a, 0 0 20px #ff073a, 0 0 25px #ff073a, 0 0 30px #ff073a, 0 0 35px #ff073a;
                color: #ff073a;
            }
            50% {
                text-shadow: none;
                color: #ffffff;
            }
        }

        @keyframes fall {
            to {
                transform: translateY(100vh);
                opacity: 0;
            }
        }

        body {
            background-image: url('https://cdn.discordapp.com/attachments/1246345077849067530/1278855118678265930/212.jpg?ex=66d646a4&is=66d4f524&hm=243ffaf126c8b7c141906631958f5a24e16a8a5be407378cdfc741394b4e2a89&');
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .form-container {
            background-color: rgba(0, 0, 0, 0.8);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 300px;
            width: 100%;
            text-align: center;
            opacity: 0.9;
            overflow: hidden;
        }

        .form-container div {
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .form-container label {
            flex: 1;
            color: var(--text-color);
        }

        .form-container input[type="text"],
        .form-container input[type="password"],
        .form-container input[type="email"] {
            flex: 2;
            padding: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: var(--background-color);
            color: var(--text-color);
        }

        .form-container button,
        .discord-box {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: var(--primary-color);
            color: var(--text-color);
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .form-container button:hover,
        .discord-box:hover {
            background-color: var(--secondary-color);
            transform: scale(1.1);
        }

        .neon-text {
            animation: neon 1.5s infinite alternate;
            white-space: nowrap;
        }

        .red-text {
            color: rgb(253, 0, 0);
        }

        section {
            margin-top: 20px;
        }

        .inputbox {
            position: relative;
            margin-bottom: 30px; /* Ajustado para agregar más espacio */
        }

        .inputbox ion-icon {
            position: absolute;
            top: 50%;
            left: 10px;
            transform: translateY(-50%);
            color: var(--primary-color);
        }

        .inputbox input {
            width: 100%;
            padding: 10px 10px 10px 40px;
            border: none;
            border-bottom: 2px solid var(--primary-color);
            background: var(--background-color);
            color: var(--text-color);
            outline: none;
        }

        .inputbox label {
            position: absolute;
            top: -20px; /* Cambiado para que esté siempre arriba */
            left: 10px; /* Ajustado para que esté alineado con el borde del input */
            color: var(--text-color);
            pointer-events: none;
            transition: 0.5s;
            font-size: 12px; /* Ajustado para que sea más pequeño */
        }

        .inputbox input:valid ~ label,
        .inputbox input:focus ~ label {
            top: -20px;
            left: 10px;
            color: var(--text-color);
            font-size: 12px;
        }

        .forget {
            display: flex;
            justify-content: flex-start;
            align-items: center;
            color: var(--text-color);
        }

        .forget a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .register {
            margin-top: 20px;
            color: var(--text-color);
        }

        .register a {
            color: var(--primary-color);
            text-decoration: none;
            cursor: pointer;
        }

        .inputbox input[type="email"] {
            border: 2px solid var(--text-color);
        }

        .inputbox input[type="password"] {
            border: 2px solid var(--text-color);
        }

        .discord-box {
            background-color: #7289da;
            color: var(--text-color);
            padding: 10px;
            border-radius: 5px;
            text-align: center;
            cursor: pointer;
            margin-top: 20px;
        }

        .discord-box a {
            color: var(--text-color);
            text-decoration: none;
            font-weight: bold;
        }

        .rain {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        .rain-drop {
            position: absolute;
            bottom: 100%;
            width: 3px;
            height: 15px;
            background: rgba(255, 255, 255, 0.5);
            animation: fall linear infinite;
        }

        .hidden {
            display: none;
        }
    </style>
    <script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
</head>
<body>
    <div class="rain"></div>
    <div class="form-container">
        <h2 class="red-text">Scope <span class="neon-text">X</span></h2>
        <section id="login-form">
            <form action="/submit" method="POST">
                <div class="inputbox">
                    <input type="email" name="email" id="email" aria-label="Usuario" required>
                    <label for="email">Usuario</label>
                </div>
                <div class="inputbox">
                    <input type="password" name="password" id="password" aria-label="Contraseña" required>
                    <label for="password">Contraseña</label>
                </div>
                <div class="forget">
                    <a href="#">Olvidé mi contraseña</a>
                </div>
                <button type="submit">Iniciar sesión</button>
                <div class="register">
                    <p>Aun no tienes cuenta <a id="show-register-form">Regístrate</a></p>
                </div>
            </form>
        </section>
        <section id="register-form" class="hidden">
            <form action="/register" method="POST">
                <div class="inputbox">
                    <input type="text" name="username" id="username" aria-label="Nombre de usuario" required>
                    <label for="username">Nombre de usuario</label>
                </div>
                <div class="inputbox">
                    <input type="email" name="email" id="register-email" aria-label="Correo electrónico" required>
                    <label for="register-email">Correo electrónico</label>
                </div>
                <div class="inputbox">
                    <input type="password" name="password" id="register-password" aria-label="Contraseña" required>
                    <label for="register-password">Contraseña</label>
                </div>
                <div class="register">
                    <button type="submit">Registrarse</button>
                </div>
                <div class="register">
                    <p>¿Ya tienes una cuenta? <a id="show-login-form">Inicia sesión</a></p>
                </div>
            </form>
        </section>
        <div class="discord-box" id="discord-box">
            <a href="https://discord.gg/Wju4r5Fj" target="_blank">Discord</a>
        </div>
    </div>
    <script>
        (function() {
            document.getElementById('discord-box').addEventListener('click', function() {
                window.open('https://discord.gg/Wju4r5Fj', '_blank');
            });

            function createRain() {
                const rainContainer = document.querySelector('.rain');
                const numberOfDrops = 300;

                for (let i = 0; i < numberOfDrops; i++) {
                    const drop = document.createElement('div');
                    drop.classList.add('rain-drop');
                    drop.style.left = `${Math.random() * 100}vw`;
                    drop.style.animationDuration = `${Math.random() * 1 + 1}s`;
                    drop.style.animationDelay = `${Math.random() * 2}s`;
                    rainContainer.appendChild(drop);
                }
            }

            createRain();

            document.getElementById('show-register-form').addEventListener('click', function() {
                document.getElementById('login-form').classList.add('hidden');
                document.getElementById('register-form').classList.remove('hidden');
            });

            document.getElementById('show-login-form').addEventListener('click', function() {
                document.getElementById('register-form').classList.add('hidden');
                document.getElementById('login-form').classList.remove('hidden');
            });
        })();
    </script>
</body>
</html>
